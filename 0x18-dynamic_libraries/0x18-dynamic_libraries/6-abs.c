#include "holberton.h"
#include <stdlib.h>
/**
 * _abs - check the code for Holberton School students.
 * @j: absolute value of an integer.
 * Return: a - absloute value of j..
 */
int _abs(int j)
{
	int a;

	a = abs(j);
	return (a);
}
